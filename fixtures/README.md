This directory provides shared fixture data for evaluation, testing, demos, and export experiments.

The fixture `data` tree is an overlay, not a complete standalone mrtdown-data repository. It contains only the files needed for current fixture cases, and may reference entities from the root `data` tree.

All included MRT lines and stations are possibly fictional.
